
// DailyStats Collection
Tinytest.add('DailyStats collection is initialized', function (test) {
  test.equal(true, true);
});
Tinytest.add('DailyStats pub/sub is initialized', function (test) {
  test.equal(true, true);
});


// Incrementation API
Tinytest.add('can increment counter isomorphically', function (test) {
  test.equal(true, true);
});
Tinytest.add('can increment counter via rpc', function (test) {
  test.equal(true, true);
});
